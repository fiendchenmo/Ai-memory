# Hermes 会话记忆 — 2026-05-29（周五）

## 工作内容

### 1. 基准测试 time stamp 注入（16:00~17:xx）
- **需求**：默默在 FMP 项目跑 single|multi 基准测试，需要 time stamp 对比耗时
- **实现**：在 `AnalysisRunner.runLlmInBackground()` 注入 `mode`/`startTimestamp`/`elapsedMs` 三个字段到 V3 JSON 输出
- **保护**：`isV3Format()` 前置判断 + try-catch 异常保护，非 V3 输出不注入
- **兼容**：字段名与 V3Result + ReportRenderer + ReportWriter 一致，单/多 Agent 路径共用

### 2. 基准测试结果对比脚本（~17:xx）
- 写 `benchmark_compare.py` — 自动遍历 `.codelens/reports/` 目录，配对单/多 Agent 报告
- 输出表格：方法数、耗时、文件大小逐文件对比
- **测试结果**（FMP 12 文件）：single 与 multi 方法数绝大多数一致

### 3. Multi-Agent 修复清单（~18:xx~21:xx）

| 修复项 | 根因 | 修复方式 |
|--------|------|----------|
| CacheConfig/noCache | 缓存策略在多 Agent 下命中旧数据 | 修复 CacheConfig 路径 |
| EDT 卡死 | 子线程操作触发了 EDT 等待 | 改用专用线程池，不依赖 EDT |
| PSI 越界（safeGetLineNumber） | 文件修改后 PSI 引用失效 | 捕获 InvalidApplicationException，返回 -1 |
| getter/setter 建索引 | 自动生成的 getter/setter 没索引 | JavaParserIndexEngine 补充注册 |
| V3 降级 | 大文件 V3 输出超限 | framework 数组 + 重试机制 |
| 框架兼容 | `toLowerCase()` 不加 Locale 在 IDEA 2021.3.1 上行为不同 | `Locale.ROOT` 显式指定 |
| 报告落盘格式化 | JSON/MD 输出格式对齐 | 统一格式化 |

### 4. 基准测试结果分析（~21:xx）
- 默默跑完完整的 12 文件 single|multi 对比
- 结果文件生成：`.codelens/reports/` 下每个文件有 `*.json`（单 Agent）和 `*_M.md`（多 Agent）
- 分析时间线：单 Agent 约 18:xx 批次，多 Agent 约 21:xx 批次
- 差异对比中有少数方法数不一致

### 5. 代码推送 + 知识图谱同步（~23:xx）
- **codelens-plugin** main 分支推送完成
- **Obsidian 知识图谱**（`D:\mo_memory\墨`）master 推送完成
- 同步内容包括所有修复清单 + 基准测试脚本

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| time stamp 字段注入 | 直接在 V3 JSON 中加 mode/startTimestamp/elapsedMs | 默默 |
| 修复范围 | 集中解决 EDT 卡死 + PSI 越界 + getter/setter + V3 降级 + 框架兼容 5 大问题 | 默默 |

## 待办

- [ ] 默默验证修复后效果
- [ ] 008 插件端开发（待 common GraphQuery 就绪）
