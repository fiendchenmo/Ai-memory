# Hermes 会话记忆 — 2026-05-19

## 工作内容

### 1. P2 Fix + 架构评审 + CR 全部完工（codelens-plugin）
- **P2 修复（4项）** ✅：FTS5 空跳过 rebuild、PSI.getQualifiedName() 判空（6处）、ToolWindow 线程安全标记关闭、清理确认
- **架构评审修复（6项）** ✅：FtsIndexer AutoCloseable、MD5 fallback 改 AssertionError、EvidenceValidator UNKNOWN 枚举、PsiExtractor 空catch+死代码、LLMClient Gson 请求体构建、LLM 指数退避重试（max3）
- **CR 代码审查修复（5项）** ✅：LOG 声明补充、escapeJson 删除、JavaParser qualifiedName 拼接包名、readStream→try-with-resources、countMethods/ResolvedCalls/calls 三重复消除、EvidenceValidatorTest 空方法补全

### 2. Schema v2 字段对齐（与喵呜 CLI 端）
- risks 加 `type(SECURITY|PERFORMANCE|MAINTAINABILITY)` 和 `impact`，severity 统一大写枚举
- keyMethods 加 `signature`、`annotations`、`calls(String[])`，移除 `complexity`
- 两端 JSON_SCHEMA 已统一，commit `4263b82`

### 3. buildBase() 三层 Prompt 方案定稿
- 插件端与 CLI 端共享 `buildBase()`（角色+JSON_SCHEMA+CORE_RULES 1-16）
- CLI 端追加规则 17-21，插件端追加 PSI 标签
- commit `474f081`

### 4. CODELENS_SPEC.md 升级 v0.1 → v0.2
- 双引擎对比报告 → 内部诊断/设置项可选
- LLM 重试策略（5xx/429/IO 最多3次，指数退避）
- Confidence 新增 UNKNOWN，BuildRequestBody 改用 JsonObject
- 重复统计方法合并至 ReportBuilder，readStream→try-with-resources
- F1 验收改为"PSI 解析报告 + FTS5 索引状态"
- 新增三级查找策略、索引范围提示，内联 JSON Schema 表格化

### 5. 与喵呜需求文档对齐
- 评估 `codelens-requirements-v1.md`，识别 7 项对齐差异 + 1 项过时验收项
- Hermes 6 项待喵呜修改（Schema 命名/UNKNOWN/buildBase/CallIndex 迁移等）
- 编写插件端补充章节 `codelens-requirements-v1-plugin-addendum.md`

### 6. 豆包 vision 模型配置
- 配置 `doubao-seed-2-0-lite-260215` 作为图片理解辅助模型，利用火山方舟免费额度
- 发现火山方舟另有 Seedream（图片生成）/Seedance（视频生成）免费额度，但 API 与 Hermes 不直接兼容

### 7. 模型策略 & 飞书网关维护
- **DeepSeek 降质谣言澄清** ✅：5月31日结束的是 v4-pro 2.5折优惠，v4-flash 价格和质量不受影响
- **模型选型确认**：Hermes/喵呜继续使用 `deepseek-v4-flash` 日常主力 + v4-pro 按次付费；Coding Plan 留给 Claude Code
- **飞书网关故障修复** ✅：WebSocket keepalive ping timeout 静默断开，执行 `systemctl --user restart hermes-gateway` 恢复

### 8. 关键提交
- `4263b82` — Schema 对齐 + UNKNOWN 置信度
- `474f081` — buildBase 方案实现

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| P2 Fix / 架构评审 / CR | 全部修复完成，M1 加固阶段验收通过 | 默默 |
| Schema v2 字段命名 | 两端 JSON_SCHEMA 统一（severity 大写/calls String[]/加 type/impact） | 默默 + 喵呜 |
| buildBase() 方案 | 三层 Prompt：公共 base + CLI 扩展 + 插件端扩展 | 默默 + 喵呜 |
| CODELENS_SPEC v0.2 | 移除双引擎对比报告，新增三级查找策略、缓存命中展示 | 默默 |
| 需求文档合并方案 | 以喵呜的 v1 为基础，Hermes 写补充章节独立存放 | 默默 + 喵呜 |
| 豆包 vision 配置 | 用 `doubao-seed-2-0-lite` + 火山方舟免费额度 | 默默 |
| 主力模型策略 | 保持 deepseek-v4-flash 日常主力，Coding Plan 留给 Claude Code | 默默 |
| DeepSeek 降质传闻 | 定性为谣言，v4-flash 定价和质量均不受 5/31 影响 | 默默 |
| 飞书网关心跳断开 | 非代码缺陷，`systemctl restart` 恢复即可，偶发网络问题 | 默默 |
| CallIndex 迁移边界 | INSERT/DELETE → common，JavaParser → CLI 端（Phase 1）；Plugin 端接入（Phase 2） | 默默 |

## 待办

- [ ] 🔴 Schema v2 字段命名 — 等待喵呜确认并统一两端 JSON_SCHEMA（阻断项）
- [ ] 🟡 UNKNOWN 置信度同步 — 喵呜 spec 未同步，等待回复
- [ ] 🟡 buildBase() 实现 — 喵呜尚未拆出公共 prompt 层
- [ ] 🟡 输出归一化层 — method_call 误放 dependencies → 映射到 keyMethods.calls（等待喵呜执行）
- [ ] 🟡 lineOffset 恒为 0 修复 — L1 失败时计算行列偏差（等待喵呜执行）
- [ ] 🟡 Prompt 强化 — CORE_RULES 加 dependencies 仅含 field 类型的约束（等待喵呜执行）
- [ ] 🟡 CallIndex 迁移执行 — 等待喵呜拍板动手（影响插件端目录索引接入）
- [ ] 🟡 跑模型样本测试 — 千问/智谱/Kimi/豆包 × 简单POJO/中等Service/Controller/Handler
- [ ] 🔄 memory 瘦身 — 任务状态类条目移入 ACTIVE.md 后删 memory 条目
- [ ] 🔄 CallIndex 迁移 + 模型特性标记 + 温度锁定（排入 Phase 2）
- [ ] 📝 当 flash 不够用时，为 Claude Code 订购千问 Coding Plan
