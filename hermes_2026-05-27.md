# Hermes 会话记忆 — 2026-05-27（周三）

## 工作内容

### 1. 角色变更：主管工作流（19:xx，CLI会话）
- **变更**：嗷呜从"兼主管+开发"正式转为"只做主管"
- **新工作流**：收到问题 → 分析根因 → 出方案 → 等默默"可以" → 派Cline执行 → 审查 → 推送
- **三条硬规则**：禁止裸改、先写后改、改完必审
- **方案必须包含**：明确注明"先构建+测试，通过才推送" — Cline不会自动做构建和测试
- **复杂问题**（2+文件/核心逻辑/线程/反复修不好）：走完整流程
- **简单问题**（错别字/配置/单文件bug）：直接改但说明
- **审查清单5项**：源码vs测试、断言放宽、删测试、范围超方案、逻辑不自洽
- **反模式**：红改绿、打圈圈、头痛医头、先推不说
- **三遍定律**：同一问题重复3次必须复盘

### 2. 编码规范（执行层，Cline/Claude Code遵守）
- 改测试必须commit标注原因
- 测试挂先查源码根因，不先改测试放水
- 禁止红改绿、禁止删除/跳过测试

### 3. AHK 基准测试自动化脚本（18:14~20:50，后续改为手动测试）
- **benchmark-automation.ahk v1.0** — 自动启动IDEA+FMP → 循环12文件 → Analyze+截图
- 第一次运行失败：AHK通过Find Action装插件太脆弱→改为文件系统解压安装zip到插件目录
- **第二次运行**：插件通过文件系统安装，脚本跑起来了，默默确认IDEA已加载FMP项目
- **最终决定（20:55）**：停止自动化，改为手动测试
- **benchmark-automation.ahk v1.1**（完善版）— 截图函数统一调独立take-screenshot.ps1，不再内嵌PS代码，增加运行前检查IDEA是否运行
- **脚本位置**：`C:\Users\dj\Downloads\benchmark-automation.ahk`
- **截图工具**：`C:\Users\dj\Downloads\take-screenshot.ps1`
- **前提**：手动打开IDEA+FMP项目后再跑脚本
- 自动化暂不启用，先手动测试

### 4. Called By 死锁修复 + 索引db重建（18:26~20:59）
- **CalledByResolver.java** 新增 `resolveByName()` — 纯SQLite查询，零PSI访问，消除ReadAction死锁
- **JavaParserIndexEngine.java** — db路径改到 `项目根/.idea/codelens/graph.db`，新增meta表持久化 `rebuild_complete` 状态
- **V3ToolWindowPanel.java** — 面板顶部三态提示：🔄 构建中 / ℹ️ 无调用方 / ✅ 有数据
- **getter/setter过滤规则修正** — 只过滤 `toString/hashCode/equals/clone/finalize`
- **测试污染修复** — `PsiGraphEngineTest.java` cleanup路径对齐
- **结果**：121测试全过 ✅ + clean buildPlugin通过 ✅
- **runIde验证**：called_by索引跑通，FMP项目某些方法显示"未找到反向调用" — 确认为正常行为（方法通过继承链调用，非直接调用，不叫bug）

### 5. Called By 接口穿透修复（P0任务，20:59~22:03）
- **需求**：字段声明是接口类型→索引存 `IUserService.findUser()`→查询用 `UserServiceImpl.findUser()`→对不上→空
- **JavaParserIndexEngine.upsertClassNode()** 填充 `implements_ifs`（JSON数组）和 `extends_class`（字符串）
- **CalledByResolver** 新增 `resolveByNameWithPenetration()` — 查自身→查接口同名方法→查父类同名方法，source标注穿透来源
- **AnalysisRunner** — 改一行调用从 `resolveByName` 切到 `resolveByNameWithPenetration`
- **已推** commit `9ae7099`
- **自动重建机制**：旧DB没meta表→createTables新建时设rebuild_complete='0'→下次runIde自动全量重建

### 6. .clinerules 更新（21:xx）
- 将完整的管理规则写入 `.clinerules`
- 包含：角色体系、主管工作流、审查清单5项、反模式、三遍定律、禁区

### 7. 增量索引孤儿键问题 + 输出退化（22:03~23:22）
- **问题表现**：runIde后第二次分析，called_by恒为空，输出比之前差了
- **根因分析**：
  - 触发层：`rebuildTriggered` 是内存集合，IDE重启后为空但`hasData()`返回true → 永远不触发全量重建
  - 数据层：`indexFile()` DELETE+INSERT 产生新ID → `method_calls_v2` 中旧callee_id成为孤儿→called_by查不到
- **两个方案**：
  - 方案A（推荐）：每次IDE会话首次分析时删除旧DB强制全量重建
  - 方案B（默默选）：`insertMethodNode` 改为UPDATE in-place保持ID不变 + `deleteFileData` 前清理孤儿边
- **测试验证中**：正在写 `testIncrementalDeleteMethod_PreservesCalledBy` 复现场景

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| 嗷呜角色 | 主管（分析→方案→审批→审查），不写代码 | 默默 |
| 方案中构建+测试 | 必须写明，Cline不会自动做 | 默默 |
| 编码规范 | 执行层遵守（改测试标原因、先查源码） | 默默 |
| AHK自动化 | 暂不启用，先手动测试 | 默默 |
| Called By接口穿透 | 走 implements_ifs + extends_class + resolveByNameWithPenetration | 默默 |
| 增量索引孤儿键修复 | 选方案B（UPDATE in-place + 清理孤儿边） | 默默 |

## 待办

- [ ] 手动测试 called_by 反向依赖 + V3 面板
- [ ] 基准测试执行（待手动测试通过后）
- [ ] 喵呜排期 common 模块（5/28~6/16）🔄
- [ ] 增量索引孤儿键修复代码推送（方案B，凌晨或次日继续）
