# Hermes 会话记忆 — 2026-05-25（周一）

## 工作内容

### 1. WSL 连接诊断 + DeepSeek 配置修复（15:19~16:30，飞书群）
- **WSL 全栈诊断**：内存 2.7Gi 空闲、磁盘 2% 使用、ping 0% 丢包、systemd running — 全部正常
- **根因定位**：`.wslconfig` 设 `networkingMode=mirrored` → Windows 网络变更时 WSL eth0 DOWN 重建
- **决策**：暂不改 NAT，下次再断时实时排查；记忆已同步 ✅
- **DeepSeek API 连接修复**：`config.yaml` 的 `providers:` 下缺 `deepseek` 条目，新会话启动时读不到 key（只在 `.env` 里有）
- **修复**：补上 `providers.deepseek.api_key` 配置，curl 验证 HTTP 200
- **教训**：DeepSeek key 必须在 `providers.deepseek.api_key` 显式声明，光写 `.env` 不够

### 2. REQ-C7 P1×3 深度审查（15:28~17:00，pro 模式）
- 审查 Claude Code 的 3 个 P1 修复，并做 pro 模式二次审查：
  - `ConstraintValidator.java` — 方法签名正则修复 ✅
  - `ConfidenceThreshold.java` — 阈值 LOW/MEDIUM 逻辑修复 ✅
  - `CrossValidator.java` — 中文关键词依赖修复 ✅
- **评估两个边界问题**（决定不修）：
  - 嵌套括号 → 🟢 低优，等真实 case
  - methods fallback 类别不匹配 → ❓ 无法评估，等具体 case
- 确认喵呜当天战果：C-9 CallIndex Phase2 ✅、C-11 ModelProfile ✅、ConfidenceThreshold ✅、#3/#4/#5 validator ✅
- **testClassStereotype 修复** → commit `a158684`，CI 106 测试全绿 ✅

### 3. PsiGraphEngine 整理 + P-2 排期讨论（15:32~16:45）
- **插件端实现全景整理**：PsiGraphEngine (726行) + CalledByResolver (157行) + PsiGraphManager + MethodSignatureUtil + CallerInfo
- **PsiGraphEngine vs common SQLiteCallIndex 结论**：
  - 插件端重度依赖 PSI，不建议同步到 CLI — 两端各走各的
  - 两个候选可抽到 common（选做）：CallerInfo.java（数据类）、MethodSignatureUtil.java（签名归一化）
  - 默默："我和喵呜确认下之后CLI是否需要同步" → 🔄 等喵呜回复
- **P-2 排期确认**：等喵呜双模式输出（C-3 SchemaVersion / C-4 SystemPrompt双模板 / C-5 Normalizer V3）就绪后开动
- 默默说"今天先到这，去养鸡了~ 明天等喵呜双模式输出就绪 → 开 P-2 V3 输出"
- **基准测试报告落盘规则**：写入 memory — 新输出就绪后跑一轮，报告落盘保存

### 4. 待办清单同步确认（20:00~20:05）
- 20:00 cron 生成喵呜待办清单（4 项待办 + 6 项已解决）
- 默默："这些在其他会话已经确认过了" — 记忆未同步的问题暴露
- WSL 结论已同步 ✅；C-3/C-4/C-5 排期和 PsiGraphEngine 同步可能有结论但看不到（可能飞书群 @喵呜 确认了）

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| WSL 网络模式 | 暂不改 NAT，下次断时实时排查 | 默默 |
| nested parentheses 边界问题 | 不做，等真实 case | 默默 |
| methods fallback 类别不匹配 | 不做，等具体 case | 默默 |
| PsiGraphEngine 同步到 CLI | 建议不同步（等待喵呜确认） | 默默说和喵呜确认 |
| 当前方向 | 等喵呜双模式输出 → 开 P-2 V3 输出 | 默默 |
| 基准测试报告 | 必须落盘保存，不只在终端输出 | 默默 |

## 待办

- [ ] 🔄 C-3/C-4/C-5 排期确认 — 默默可能已通过飞书群跟喵呜确认，我这边看不到
- [ ] 🔄 PsiGraphEngine 是否需要同步到 CLI — 等待喵呜回复
- [ ] ⏳ 模型特性标记 (ModelProfile) — 排期 5/29，喵呜已认领
- [ ] ⏳ CallIndex 迁移 — 代码已合入 main (78ea679)，SQLite 迁入 common 排期 5/30~5/31
- [ ] ⏳ 等喵呜双模式输出（C-3 / C-4 / C-5）→ 开 P-2 V3 输出 + 基准测试落盘
