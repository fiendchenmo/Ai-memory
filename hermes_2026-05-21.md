# Hermes 会话记忆 — 2026-05-21

## 工作内容

### 1. 多模型基准测试（全天 — 正式收官 🎉）
- **A组 DeepSeek v4-flash** — 全量12文件完成，L1通过率100%（12/12），行号精确率100%，基准FinalScore 77.2/100
- **B组 DeepSeek v4-Pro** — 仅跑T1：3x慢+卡IDE+质量无明显提升，**放弃全量**，仅适合单文件架构攻坚
- **C组 Qwen-Plus 抽样** — T1/T2/T6/T11完成，L1通过率100%，但输出精简（Deps少、项数少），Flash更优
- **D组 豆包 doubao-seed-2-0-lite-260215** — 抽样T1/T2/T6/T11完成，质量接近Flash，可用免费备选
- **最终三模型对比**：**Flash 79.4** > 豆包 79.0 > Qwen-Plus 78.6
- **结论**：Flash保持日常主力不动，豆包免费备选，Qwen-Plus不推荐（付费且不优于Flash）
- 基准测试规范v2.0更新（12文件定稿 + 禁用缓存 + 简化执行流程）
- `session-fatigue` skill 创建部署
- ACTIVE.md 更新为基准测试收官状态 + 疲劳提醒

### 2. Round 1 双向对齐验证（CLI端3修复生效）
- CLI端3项修复全生效：
  - **keyMethods验证口径统一**：偏差0率 ~48.5% → **100%** ✅
  - **Normalizer method_call过滤**：C4 deps 6→3（全field类型）✅
  - **max_tokens 8192→16384**：C2/C10 无截断 ✅
- **整体指标**：L1通过率 89.9%→**99.7%**，偏差0率 ~48.5%→**100%**，Deps匹配率 86.2%→**99.6%**
- 插件端保持稳定无新问题
- 仅存1个问题：C6 deps[6] 字段名 `amsBillAssetsService`→`amsBillAssetsAttrService`（LLM轻微幻觉，待确认根因）

### 3. 插件端 CI 配置（commit `0fe9f50`）
- 创建 `.github/workflows/ci.yml`：`checkout → JDK 11 → chmod +x → test → verifyPlugin → buildPlugin → upload artifact`
- 对齐喵呜 CLI 端 CI 结构：加 `v*` tag 触发 + `notify-on-failure` job
- commit `0fe9f50` — Phase 2 kickoff

### 4. JitPack 依赖升级 v0.2.2 → v0.2.3
- `build.gradle` 更新依赖版本（含 Few-shot 规则 23-25）
- 编译 + 全部 40+ 测试通过 ✅

### 5. Cline 入职 & `.clinerules` 配置
- 编写插件端 `.clinerules`（技术栈/JDK 11/Gradle/JUnit 4/构建命令/禁区/工作流程/角色定义）
- AGENTS.md 更新：Cline → 全能助手（非编程），Claude Code → 编程搭档

### 6. 飞书线程回复修复（延续20日会话）
- Gateway `_reply_anchor_for_event()` 修复：飞书线程消息走 ReplyMessage 留在原线程，不再开新线程
- 恢复卡片消息解析增强 `_dump_card_summary()` 兜底逻辑
- 默默纠正了「卡片解析增强」vs「Tab页展示」的认知混淆 ✅

### 7. 🔴 豆包 vision key 飞书群泄露（安全事件）
- 根因：每日晚间总结 cron job 将豆包 vision key 明文写入输出，自动发到飞书"工作室"群
- 修复：✅ cron prompt 加安全规则（禁止输出任何 key/token/密码明文）
- 修复：✅ memory 中所有 key 明文替换为仅保留模型名
- **建议**：去 Volcengine Ark 控制台 revoke 旧 key，重新生成

### 8. 飞书长连接状态排查（21:09~21:28）
- WebSocket 长连接 ✅ 活跃（最后连接 14:26:04），Gateway 进程 ✅ 运行中
- 21:09~21:14 连续5次发送失败 [99992402] field validation failed
- 根因：lark_oapi 内部 access_token 过期后自动续签失败
- 21:28 WebSocket 断线重连 → 客户端重建 → 问题自行恢复
- ⚠️ 潜在风险：~7小时周期性复发，建议加 token 定期刷新机制

### 9. 管理复盘笔记同步 @喵呜
- 默默发了《2026-05-22-管理复盘笔记.md》到飞书群 @喵呜（边界/跨端表格/审查粒度/三遍定律）
- 喵呜待同步至 CLAUDE.md

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| 多模型基准测试结论 | Flash 最优(79.4)保持主力，豆包(79.0)免费备选，Qwen-Plus不推荐 | 默默 |
| B组 Pro 放弃 | 3x慢+卡IDE，仅适合单文件架构攻坚 | 默默 |
| 基准测试收官 | 不再跑更多模型，进入 Phase 2 排期 | 默默 |
| 豆包模型 | doubao-pro-32k 未开通 → doubao-seed-2-0-lite-260215 | 默默 |
| Round 1 验证通过 | CLI端3修复全生效，整体稳定（99.7% L1通过率） | 默默 |
| 插件端 CI | 我直接写 YAML，无需默默/喵呜介入 | 默默 |
| 飞书线程回复 | 线程里 @我 → 走 ReplyMessage 留在原线程 | 默默 |
| cron prompt 安全 | 禁止输出任何 key/token/密码/secret 明文值 | 默默 |
| Git 历史清理 | 建议方案B（废弃key，不推filter-repo），等默默决定 | 默默 |
| Cline 工作分配 | 等喵呜出编码规范后 review，再分配第一个 task | 默默 |

## 待办

- [ ] 🔴 去 Volcengine Ark 控制台 revoke 泄露的豆包 vision key，生成新 key
- [ ] Git 历史清理（CodeLens.txt 含4个 key）— 等默默决定废弃 key 还是 git filter-repo
- [ ] Phase 2 排期（CallIndex迁移 + ProviderPreset温度锁定）
- [ ] C6 deps[6] 字段名不匹配根因确认（LLM幻觉 vs 底图漏提取）
- [ ] 🔄 Cline 编码规范 review — 等喵呜初版出来后 review 补充
- [ ] 🔄 喵呜待回复（管理复盘笔记同步、JavaParser底图漏提取、CLI端CI配置、JitPack JDK版本、需求#5/#6/#7、common独立模块分割）
- [ ] ⚠️ 飞书长连接周期性 token 刷新 — 建议加定时 client 重建机制
