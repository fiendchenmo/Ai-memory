# Hermes 会话记忆 — 2026-05-26（周二）

## 工作内容

### 1. skill-creator 元技能 + self-evolve 方向修正（11:52，飞书群）
- **阿里云Agent Skill规范调研**：研究了阿里技术博客的Agent Skill设计，确认与Hermes Skill体系同源（agentskills.io 开放标准）
- **skill-creator 元技能创建完成**：Pipeline四阶段（收集→推荐→生成→创建），含批量模式（auto-generate YAML + batch-create.py）
- **默默纠正方向**：批量模式偏离了"自进化"初衷 — 仍然需要默默写YAML文件。正确方向应该是基于一周会话记录自动进化
- **清理**：删除 `auto-generate/`、`batch-create.py`；归档 `cross-agent-code-review` 到 `skills_archive/2026-W21/`
- **self-evolve 技能补全**：增加了具体查询模板（3组纠正搜索+4组重复模式搜索）、自动生成规则（description/body从纠正上下文提取规则）、infrastructure白名单（awareness-engine/long-term-memory-archive/self-evolve/skill-creator不可归档）、cron配置（周日08:30运行）
- **周日自动进化流程**：08:30 self-evolve → 09:00 SOUL重生，互不冲突

### 2. 反向依赖（Called By）UI退化修复（18:19~19:xx，CLI会话）
- **问题**：called_by查不出来，UI退化成纯文本（渲染失败fallback到文字模式）
- **根因定位**：多个问题叠加
  - 后台线程操作PSI缺少 ReadAction 包裹 → 爆 "Read access only from EDT"
  - CalledByResolver.getLineNumber() 使用 getTextOffset() 返回字符偏移量而非行号
  - PsiGraphManager.indexFileAsync 缺少 ReadAction → 全量重建时爆 IllegalAccessError
- **修复方案**：
  - CalledByResolver + V3ToolWindowPanel 加入后台线程 ReadAction.protect 和 ProgressManager
  - `CalledByResolver.getLineNumber()` 改用 `split("\n").length` 计算正确行号
  - Add diagnostic logging with `[called_by]` prefix for all failure paths

### 3. 三大Bug修复 + P-4上线（18:19~19:xx）
- **Fix 1 — AnalysisRunner传null修复**：改用 `PsiGraphManager.getEngine(project)` 不传 null
- **Fix 2 — Getter/setter过滤规则修正**：从 `startsWith("get")/startsWith("set")` 改为按参数个数过滤（setter→1 param, getter/is→0 param）
- **Fix 3 — SQLite驱动显式加载**：`PsiGraphEngine.open()` 加 `Class.forName("org.sqlite.JDBC")`

### 4. graph.db 重建 + getTextOffset(-1) 修复（20:27~21:xx）
- **问题**：graph.db 全量重建报 "begin 0, end -1, length XXX"，根因是 Lombok 合成类 getTextOffset() 返回 -1
- **修复**：PsiGraphEngine getLineNumber/getEndLineNumber 加入边界检查（offset<=0 → return 1）
- **ReadAction 卡死**：`ReadAction.run()` 包住整个 `rebuildGraphDb()` 导致大项目全量索引时 EDT 被阻塞
- **修复**：去掉自动触发 rebuildGraphDb，只在 ReadAction 中包单文件 indexFile（快速释放）
- **called_by 静默失败**：CalledByResolver 所有 catch 都静默了，加上 Logger 诊断日志（log 搜 `[called_by]` 即可定位）

### 5. 基准测试（20:27~21:xx）
- 默默在 IDEA 启动 runIde，手动跑 T1-T12
- T10 PmsBillButtonHandler 通过 ✅（Called By 数据完整）
- T11 PaymentResponse 通过 ✅（所有方法显示 Line: 1 — Lombok 代码生成固有现象）
- T10 后 IDE 卡死（ReadAction 包全量重建问题），之后修复

### 6. 喵呜待办清单（20:01 cron）
- 今日无新增喵呜待办
- C-3/C-4/C-5 评审通过 ✅
- 模型特性标记 (ModelProfile) 已合入 main (78ea679) ✅
- REQ-C7 P1×3 修复全部完成 ✅

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| self-evolve 方向 | 基于session_search自动进化，不需要默默写YAML | 默默 |
| Getter/setter过滤 | 按参数个数判断（set→1, get/is→0），不按前缀 | 默默 |
| called_by 线程模型 | 后台线程 + ReadAction + ProgressManager，不在EDT | 默默 |
| 全量重建触发时机 | 去掉自动触发，只增量索引 | 默默 |
| OpenRouter DNS被墙 | 走本地Hermes gateway (127.0.0.1:8642) | 默默 |

## 待办

- [ ] 收基准测试完整结果 → 分析报告写到 `docs/benchmark/`
- [ ] 回答默默问题：反向依赖点击行号跳转目标文件 — 已实现（P-4），非遗漏
- [ ] 基准测试重新执行（修复了graph.db + called_by问题后）
- [ ] EntroCamp L3 学习（反馈吸收与行为修正）
- [ ] 5/31（周日）self-evolve 第一次自动运行
- [ ] 🔄 C-3/C-4/C-5 排期确认 — 默默可能已与喵呜确认
