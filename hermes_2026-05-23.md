# Hermes 会话记忆 — 2026-05-23

## 工作内容

### 1. Phase 2 方案设计（全天主力）

- **PsiGraphEngine v1.0 技术方案设计 + 评审通过** — `docs/architecture/psi-graph-engine.md`（879行）
  - 4 层架构：Layer 0 PSI缓存 → Layer 1 图索引器 → Layer 2 SQLite邻接表 → Layer 3 加权上下文提取
  - method_nodes + class_nodes 双节点表，qualified_sig + content_hash 仲裁 UNIQUE 冲突
  - method_calls_v2 加 confidence 三级（RESOLVED/LIKELY/UNRESOLVED）
  - FTS5 content=method_nodes 挂载，增量触发（content_hash），迁移策略（index.db → graph.db，保留降级）
  - 交付估计：4 天（含9个测试Case）

- **003 需求文档完成** — `docs/requirements/003-psi-graph-engine.md`，等待默默评审

- **GitHub CodeGraph 项目调研** — 重点分析 CGC（Python+KuzuDB+MCP Client）、xnuinside/codegraph（轻量Python）、codegraph-rust（Rust+SurrealDB），CGC 的图库选型和 agentic 工具设计最有参考价值

- **新 LLM 输出结构 v3 设计**（等后期实现）：
  - 代码顺序驱动：summary → fields → methods（每个方法含 params/description/logic_summary/calls/return/risks/exceptions）
  - methods[].risks 替代顶层 risks[]，fields[] 替代 dependencies[]

- **包级/模块级分析设计**（等后期实现）：
  - 右键目录自动判断包级/模块级/混合视图
  - 包内依赖图 + 跨包依赖 + 循环依赖检测
  - 不跑额外 LLM，数据从单文件分析聚合

- **Common 模块跨端协调** — 4条变更记录在 `common-change-tracker.md` + commit `6eb94b6`：
  - 移除 architecture_issues、EvidenceValidator methodRanges、SystemPrompt [FACT]/[INFER]、JSON Schema 版本化 + Normalizer

- **喵呜交付排期已落实**：
  - 5/24：移除 architecture_issues
  - 5/26：ProviderPreset 温度锁定
  - 5/27：EvidenceValidator + methodRanges
  - 5/27~28：JSON Schema 版本化 + Normalizer
  - 5/28：SystemPrompt [FACT]/[INFER]

- **永无农场信息写入 memory**（清理 KuzuDB 归档记录和 WSL 编码乱码记录腾空间）

### 2. EntroCamp Day 2 课程（23:00~23:30）

| 时间 | 科目 | 课题 | 评分 | 进度 |
|------|------|------|------|------|
| 23:00 | 🧩 推理与判断 L2 | 多因素决策分析（加权决策矩阵6步法） | ⭐ 5/5 | 2/3 |
| 23:03 | 🧩 推理与判断 L3 | 逻辑谬误识别与反驳（钢铁侠论证五步法） | +10 cls | **3/3 🎓 毕业！** |
| 23:15 | 🎯 读懂意图 L2 | 隐性意图推理（双层意图模型） | +10 | 2/3 |
| 23:31 | 🧠 记忆与学习 L2 | 持久化记忆策略（五步 SOP + 三条件过滤法） | +10 | 2/3 |

- 推理与判断全科毕业 🎉，产出 SKILL 草稿
- 所有课程笔记保存至 `/tmp/EntroCamp学习笔记/`
- 核心收获：钢铁侠论证（先假设对方是对的再找漏洞）、决策矩阵加权法、双层意图模型、记忆三条件过滤法

### 3. 晚间互动

- 21:00 默默说"没有了"→ 嗷呜休息
- 22:58 默默问"今天晚上还有课吗？"→ 嗷呜一时没想起来 → 默默"？？？"→ 嗷呜查 session 后想起
- 默默"刚好，去看看呢"→ 确认课程已通过 cron 自动上

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| PsiGraphEngine 启动 | 等默默评审 003 需求文档后 Cline 启动（4天计划） | 默默 |
| v3 新输出结构 + 包级分析 | 暂不实现，喵呜说 CLI 端能力不够，等 v3 统一 | 喵呜 |
| 光标跟随代码滚动 | Phase 2 不做，等后期 | 默默 |
| 插件端 ↔ 喵呜并行 | 无阻塞关系，各干各的 | 默默 |
| 永无农场 API | 信息写入 memory（替换 KuzuDB 归档记录） | 默默 |

## 待办

- [ ] 🔄 默默评审 003 需求文档 → 通过后飞书通知嗷呜 → 写测试用例 → Cline 开工
- [ ] 喵呜从 5/24 起分批交付 common 变更（5项）
- [ ] EntroCamp Day 3（5/24 23:00）：🎯 读懂意图 L3 + 🧠 记忆与学习 L3（两科毕业）
- [ ] 推理与判断行为准则 — 等默默审批后写入记忆
