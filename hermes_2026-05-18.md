# Hermes 会话记忆 — 2026-05-18

## 工作内容

### 1. 配置优化
- 飞书群关闭 `tool_progress` 和 `interim_assistant_messages`
- `gateway_notify_interval` 改为 600s（10分钟不重复通知）
- 飞书回复模式改为 `create_message`（不发话题线程），`_reply_anchor_for_event` 修改后重启生效

### 2. M1 加固 — P0/P1 修复（codelens-plugin）
- **P0-5 重复IO修复** ✅：`CodeLensAnalyzeAction` 3次磁盘读源文件 → 统一 `PsiFile.getText()`（内存读），5文件修改，构建测试均通过
- **P0-6 原生库沙箱修复** ✅：`FtsIndexer` SQLite 解压路径从 `projectDir/.codelens/native` → `PathManager.getPluginTempPath()`，沙箱兼容，构建测试均通过
- **P1-4 空 catch 块** ✅：`FileSystemCache` 两处 + `FtsIndexer.close` 一处，添加 `LOG.warning`
- **P1-8 LRU 缓存淘汰** ✅：`Cache.java` / `CacheConfig.java` / `FileSystemCache.java` — LRU（文件最后修改时间）+ maxEntries=1000，已完成
- **P1-10 校验器集成** ✅：L1/L2 校验器集成完成
- **P1-5 God Class 拆分** 🔄：`PsiExtractor`(249行) / `ReportBuilder`(245行) / `AnalysisRunner`(142行) 已创建，但**未编译验证**

### 3. 方案评估 & 决策
- **方案 A vs B**：方案B（批量分析+卡片展示+调用关系图，P2→P3）优先，方案A（多Provider LLM）排 P2
- **X1 SystemPrompt 共享** ✅ — `SystemPrompt.java` 在 `codelens-common/prompts/`，插件端直接调 `SystemPrompt.build()`
- **X3 缓存策略** ✅ — TTL=7天, maxEntries=1000, MD5+版本号(P1-6)，版本变更时旧缓存自动失效
- **M2.2 Schema对齐** ✅ — 沿用 `CodeMetaData.JSON_SCHEMA`，批量分析产出 `.codelens/results/*.json` 格式一致
- **X4 CLI代码清理** ✅ — `EvidenceValidator`/`ConfidenceAnnotator` 本地重复已删（M-P0-3），改import common；`ColorUtil` Phase 2 移回 CLI（不急）
- **CLI端多Provider（Phase 2）** ✅ — 喵呜拍板：`LLMClient.analyze()` 参数透传，CLI 加 `--provider`，`ProviderPreset` 放 common 两端共享

### 4. 长期记忆归档方案
- 三层检索：memory（毫秒）→ session_search（秒级语义）→ grep hermes_*.md（秒级）
- 每日 **23:55** cron 定时归档 + git commit（原定 21:55，后改为 23:55 避免漏晚间对话）
- `hermes_INDEX.md` 索引维护，90天后按月压缩
- 记忆满时手动追加到当天 `hermes_YYYY-MM-DD.md`，cron 合并补充

### 5. 沙箱 IDE 运行（技术排查）
- coroutines-agent jar 缺失导致 `runIde` 失败
- IDE 内 Gradle 面板无法传递 `-x test` 参数
- 解决：WSL terminal 直接 `gradle runIde --no-daemon -x test`

### 6. 关键提交
- `cc4e50c` — P0-5 / P0-6 / P1-4 / P1-8 / P1-10 / P1-5 / 多Provider 合并提交，+1816/-1018 行，19文件
- `40601b2` — CLI 端本地重复类（EvidenceValidator/ConfidenceAnnotator）清理

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| 飞书消息格式 | 简洁结构化，只发最终更新 | 默默 |
| 卡片模式优先级 | 代码审查→基准测试→M2.2批量分析+卡片→调用关系图 | 默默 |
| 长期记忆归档 | `C:\Users\dj\long-term-memory\`，23:55 cron，三层检索 | 默默 |
| P0-5 读取策略 | 统一 PsiFile.getText()（内存读），消除磁盘 IO | 默默 |
| P0-6 原生库路径 | `PathManager.getPluginTempPath()`，沙箱兼容 | 默默 |
| P1-4 空 catch 处理 | 添加 LOG.warning，不吞异常 | 默默 |
| 方案 A vs B | 方案B（批量分析+卡片+调用图）优先，方案A 排 P2 | 默默 |
| X1 SystemPrompt | common 模块共享，直接调 build() | 喵呜 |
| X3 缓存策略 | TTL=7天, maxEntries=1000, MD5+版本号 | 默默 |
| M2.2 Schema | 沿用 CodeMetaData.JSON_SCHEMA | 默默 |
| X4 CLI清理 | 本地重复已删，ColorUtil Phase 2 | 喵呜 |
| CLI多Provider Phase 2 | LLMClient 参数透传 + CLI --provider + ProviderPreset common | 喵呜 |
| 群内称呼 | @嗷呜 = Hermes, @喵呜 = CLI端同事 | 默默 |

## 待办

- [ ] P1-5 God Class 拆分 — 编译验证（新类 PsiExtractor/ReportBuilder/AnalysisRunner）
- [ ] 清理 `readTimestamp()` 方法（不再用于淘汰）
- [ ] memory 瘦身：任务状态类条目移入 ACTIVE.md 后删
- [ ] M1.1 安全修复（API Key 加密存储）
- [ ] M1.2 剩余重构任务
- [ ] M1.3 测试补全
- [ ] M1.4 构建加固
- [ ] 代码审查收敛
- [ ] 基准测试
- [ ] M2.2 批量分析 + 卡片展示
- [ ] V2 调用关系图
- [ ] 方案A 多 Provider LLM 支持（P2）
- [ ] ColorUtil 移回 CLI（Phase 2）
