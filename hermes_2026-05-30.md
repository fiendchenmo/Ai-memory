# Hermes 会话记忆 — 2026-05-30

## 工作内容

### 上午/下午

1. **Coze Bridge 配对** — 默默通过 Coze 平台连接 WSL Agent，执行 2 次 `npx coze-bridge` 配对（不同 pair-code），配对均成功。后续 Coze 平台报错 `agent spawn failed 3 times (code: -32004)`，属于 Coze 平台自身问题。

2. **GitHub SSH 443 端口配置** — `D:\mo_memory\墨` 仓库 SSH 22 端口不通，全局配置 GitHub SSH 走 443：
   - Windows `C:\Users\dj\.ssh\config` 配 `HostName ssh.github.com` + `Port 443`
   - WSL `/home/dj/.ssh/config` 同步配置
   - 已验证 `ssh -T -p 443 git@ssh.github.com` 通过 ✅
   - Windows 443 端口被 Steam++ 劫持，必须用 `ssh.github.com` 绕过

3. **长期记忆仓库修复** — 发现 5/27 补充 commit 未 push，远程落后本地 1 commit。已 push 挂起 commit ✅，443 端口配置后预期 cron 能恢复

### 下午/晚上

4. **补写 5/28~5/29 归档文件** — 默默上线问状态时发现 5/28~5/29 的归档文件因 cron SSH 22 端口不通未生成。从 session DB 恢复内容并补写 `hermes_2026-05-28.md` + `hermes_2026-05-29.md`，commit `5b76831` 已 push

5. **喵呜完整排期 → 插件端排期（3 commits → codelens-plugin）**
   - `0555b0c` — 插件端完整排期 ROADMAP + 009 QA 历史 Schema（`docs/requirements/009-qa-history-records.md`）
   - `de9305b` — Phase7.5 分工修正
   - `df82bb4` — 最终分工确认推送

6. **三个关键决策确认（默默 ✅）**
   - Phase6.5 Diff 影响分析 → **纯 CLI 算，插件只展示**
   - Phase7.5 CodeQA → **工具窗口内嵌 Chat Panel**
   - 知识图谱可视化 → **WebView 主力 + Swing 降级**

7. **Phase7.5 最终分工**：CodeQA Agent（意图→扩散→源码→回答）+ 包级 QA 历史注入协议格式 → **喵呜**；Chat Panel UI + 上下文组装 + QA 历史存储/匹配 → **嗷呜**

8. **009 QA 历史需求文档** — 默默问"用户之前问过的问题能不能存下来复用"，延伸出 009 需求文档。包级 QA 历史存储到 `.codelens/reports/qa/`，n-gram 精确匹配引擎，qa_index.json 索引。两层命中策略：精确匹配直接返回 → 未命中时 TOP5 包级 QA 历史注入 prompt

## 关键决策
| 决策 | 结论 | 拍板人 |
|------|------|--------|
| GitHub SSH 走 443 | 全局配置 `.ssh/config`，HostName 用 `ssh.github.com` | 默默 |
| Phase6.5 Diff 分析 | 纯 CLI 算，插件只展示 | 默默 |
| Phase7.5 CodeQA UI | 工具窗口内嵌 Chat Panel | 默默 |
| 知识图谱可视化 | WebView 主力 + Swing 降级 | 默默 |
| Phase7.5 分工 | 喵呜：CodeQA Agent+注入协议格式；嗷呜：ChatPanel+存储+匹配 | 默默 |
| 归档 cron job 待定 | 等待默默确认（保留现状/清理/手动验证） | — |

## 待办
- [ ] 默默 runIde 验证多 Agent + 报告落盘（main 分支已就绪）
- [ ] 008 需求文档评审（全包分析 + 报告落盘）
- [ ] 喵呜排期 common 模块 (6/7~6/16)
- [ ] common 知识图谱 Schema 完成后启动 008 插件端开发
- [ ] 待默默确认归档 cron job 状态（清理保留 vs 留现状 vs 手动验证）
