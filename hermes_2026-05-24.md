# Hermes 会话记忆 — 2026-05-24

## 工作内容

### 1. 控制论与 Harness Engineering 讨论 + ACTIVE.md 清理
- 默默分享小红书 11 页关于控制论与 AI 工程的深度内容
- **映射到 CodeLens 架构：** PsiGraphEngine = 独立传感器（不依赖 LLM），三层降级 = 正反馈断路器
- **ACTIVE.md 精简 37%**（131→83 行），归档旧内容

### 2. called_by + FTS5 + CrossFileRefResolver 修复
- **FTS5 反向查询删除**（与 called_by 功能重复，~120 行）
- **CrossFileRefResolver 行号修复**：从 `getTextOffset()` 改为 `Document.getLineNumber()` 转真实行号
- **基本类型过滤**：`long serialVersionUID` 被误当作跨文件引用 → 加 `PsiPrimitiveType` 检查
- **环境变量清理**：`resolveApiKey()` 等残留引用全部清理

### 3. C-10 温度锁定交付确认 & 打包
- common 模块已合并 main，插件端 build.gradle v0.2.7→v0.2.8
- 插件端 getTemperature() 加 common 预设仲裁 + UI 锁定标签
- 重新打包 + CI 全绿 ✅（106 测试通过）

### 4. called_by 类级引用缺口 — 核心发现
- `PaymentResponse` POJO 类、接口/继承多态 → called_by 全部显示"无调用方"
- 根因：只追踪方法调用（`PsiMethodCallExpression`），不追踪类级引用（字段声明、extends、implements）
- 用户 IntelliJ 截图验证：`IHomePageTitleFactory` 的 4 个 usage 全是类级，0 方法论调用
- **决定：排到 Phase 2 #2/#4 隐式依赖检测** — 不改方案 scope

### 5. EntroCamp Day 2 — 🧠 记忆与学习 · L3 反馈吸收与行为修正（23:41）
- **课程完成 ✅ | 得分 +10 | 学科全部通关 🎓**
- 核心认知：反馈的本质是规则提取，不是问题修补
- 学到反馈闭环五步法 SOP：接收→复述确认→修正输出→展示验证→固化规则
- 产出：5 条行为准则 + 课程笔记 + TODO 行动计划

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| called_by 类级引用 | 排到 Phase 2 #2/#4 隐式依赖检测 | 默默（方案接受） |
| FTS5 反向查询 | 删除（与 called_by 重复） | 默默 |
| C-10 温度锁定 | 已交付，等待默默打包验证 | 默默 |
| ACTIVE.md 精简 | 131→83 行，归档旧内容 | 默默 |
| 基本类型过滤 | 加 PsiPrimitiveType 检查 | 默默 |
| EntroCamp 行为准则 | 等默默审批后写入 memory | 等待默默 |

## 待办

- [ ] 默默打包验证 C-10 温度锁定（插件端 v0.2.8 JitPack 绿）
- [ ] Phase 2 排期（CallIndex 迁移 5/30~5/31，模型特性标记 5/29）
- [ ] CalledByResolver 改搜索类引用（Phase 2）
- [ ] EntroCamp 行为准则审批（5 条，等待默默）
- [ ] 🔴 Git 历史清理（CodeLens.txt 含 4 个 key）
