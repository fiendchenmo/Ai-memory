# Hermes 会话记忆 — 2026-06-01

> ⚡ 周一。14:02 Sandbox锁排查 + PackageSummaryPanel乱码 + 19:57 called_by验证 + 22:00 打包编译调试

## 工作内容

### 1. Sandbox 锁问题说明 + 构建工作流 Skill
- 用户问打包1小时的原因：runIde 沙箱残留进程（java.exe/idea64.exe）持有 `build/idea-sandbox/` 文件锁，prepareSandbox 无法覆盖写入
- Gradle wrapper.jar 损坏（ClassNotFoundException），从 `.bak` 恢复
- 创建 **`codelens-build-workflow` skill**：taskkill 杀进程 → rm -rf idea-sandbox → 修复 gradle-wrapper.jar → buildPlugin
- 核心流程：① taskkill /f /im java.exe + idea64.exe ② rm -rf build/idea-sandbox ③ ./gradlew buildPlugin

### 2. PackageSummaryPanel 中文乱码排查
- user 发截图：包级聚合面板中文显示为方块乱码
- 排查方向：字体 Fallback 问题（IDE 渲染时找不到对应中文字形）

### 3. PackageSummaryPanel 水平滚动条修复
- `PackageSummaryPanel.java` — `HORIZONTAL_SCROLLBAR_NEVER` → `HORIZONTAL_SCROLLBAR_AS_NEEDED`
- 默默确认后改，防止内容超宽时截断不可见

### 4. PSI Fallback for called-by 验证 + 编译调试
- called_by 在 runIde 中验证通过：用户反馈"有结果了，日志可见 callindex.db hit"
- 后续尝试进一步调试时遇到 Gradle 编译问题：
  - Windows 侧 gradlew.bat 超时（120s timeout）
  - WSL 侧编译后的 `.class` 文件不包含新代码（PsiGraphManager 中 PsiGraphIndexBuilder 引用未出现）
  - 尝试了 clean build + --no-build-cache 仍不生效
  - 22:07 `buildPlugin` 成功（1m34s），但 instrumented jar 中 class 仍是旧代码
  - 会话以 timeout + 工具循环告警结束，未找到根因

### 5. 20:00 喵呜待办清单（cron）
- common v0.6.1 BFS限流 + fileLayers 已合入 main，已打 tag → ✅
- CodeQA 分工方案（Agent+协议=喵呜，存储+UI+上下文=嗷呜）待确认默默是否已发给喵呜 → ⏳
- Phase6.5/7/7.5 后续排期待拍拍板 → 📌

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| 构建工作流 | 创建 codelens-build-workflow skill，先杀进程再打包 | 默默 |
| 水平滚动条 | HORIZONTAL_SCROLLBAR_NEVER → AS_NEEDED | 默默 |

## 待办

- [ ] called_by 编译不更新 class 的根因继续排查（PsiGraphManager 修改后 class 不变）
- [ ] Phase6 PackageSummaryPanel 中文乱码修复
- [ ] CodeQA 分工方案等待默默确认是否已发给喵呜
- [ ] Phase6.5/7/7.5 后续排期（等待默默拍板）
