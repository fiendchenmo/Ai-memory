# Hermes 会话记忆 — 2026-05-20

## 工作内容

### 🟢 主线任务：JitPack 接入 + 评审修复全量（P0/P2）
- **第一轮（788ae9c）**：build.gradle 加 JitPack 依赖，删除 14 个 common 副本文件
  - 任务 1：缓存写入前校验结果有效性 — `isValidLlmResult()`（null/空/❌/JSON）
  - 任务 2：`\r\n` 换行符污染处理 — `normalizedSource = sourceCode.replace("\r\n", "\n")`
  - 任务 3：删 SummaryCache.java（无业务引用）
  - 任务 4：ReportBuilder 规则修复 — 改用 `SystemPrompt.buildBase()`，去重规则
  - 编译 + 测试：**96/96 全绿** ✅
- **JitPack 阻塞**：CLI 端 JDK 版本问题（Files.readString 是 JDK11+），临时用本地 mvn install + 扁平 pom 替代

### 🟢 飞书网关修复
- 修复飞书线程回复问题 — `base.py` 的 `_reply_anchor_for_event()` 对飞书线程消息返回 `None`，导致 Gateway 走 CreateMessage 开新线程
- 恢复卡片消息解析增强 — `feishu.py` 的 `_dump_card_summary()` 兜底逻辑
- 试验 lark-oapi 1.5.3→1.6.4 升级以解决 `99992402` 字段校验错误（未最终解决）
- 默默纠正了「卡片解析增强」和「Tab页展示」的认知混淆 ✅ 已修正记忆

### 🟢 喵呜 JDK 修复 + JitPack v0.2.1 直连（cf1279d）
- 喵呜修复了 CLI 端 JDK 兼容：`Files.readString()` → `new String(Files.readAllBytes())`，4 处全改，JDK7+ 兼容
- 喵呜打了 tag v0.2.1，JitPack 三个模块全构建成功（common jar HTTP 200 可下载）
- 插件端切换：去掉 `mavenLocal()`，依赖坐标 v0.2.0 → v0.2.1，直连 JitPack
- 编译 + 测试通过 ✅ commit `cf1279d`（push 因 GitHub 网络问题暂挂）

### 🟢 项目记忆同步 + ACTIVE.md 对账
- 喵呜确认了 6 项待办状态：
  - #1 JitPack 接入 ✅ 已就绪（v0.2.1 直连）
  - #2 calls 格式归一化 ✅ 已实现（OutputNormalizer），接入 common 即可
  - #3 lineOffset 恒为 0 ⏸️ 不改校验器，等 Round 0 数据
  - #4 Prompt 强化 ✅ CORE_RULES 17-22 已覆盖
  - #5 common 分割 ✅ JitPack 已解决
  - #6 methods fallback ⏸️ 需具体 case，当前设计合理
- ACTIVE.md 全面更新，P2 待办已压缩

### 🟢 其他
- Hermes v0.13.0 → v0.14.0 升级分析（808 commits · 633 PR，建议升级，需注意 stash 冲突）
- 默默表达风格分析 → 宫本武藏（一刀流、结构降维、尊重边界）
- 基准测试规划：各测各的 + 交叉对比结果，默默已发样本清单到飞书群

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| Hermes v0.14.0 升级 | 建议升级（有本地改动需 stash），Foundation Release | 默默待执行 |
| 基准测试分工 | 插件端(Hermes) + CLI端(Claude Code)各测各的，同一批样本，交叉对比偏差模式 | 默默 |
| OutputNormalizer 工具类过滤 | Prompt 规则 19 + OutputNormalizer 双层过滤 → deps 稳定在 15-30 个 | 默默 |
| 三层交叉验证 | 全量落地完成，两端对齐 | 默默 |
| 需求 #5/#7 已实现 | OutputNormalizer + CORE_RULES 17-22 已覆盖，无需额外方案 | 喵呜 |
| JitPack 独立模块 | JitPack 发布 common 为独立 jar，不再需要分割方案 | 喵呜 |
| lineOffset 恒 0 不改 | 不改校验器，跑 Round 0 基准数据再定 Prompt 优化方向 | 喵呜 |

## 待办（当前状态）

### 🔴 P0 — 明天开跑
- [ ] **跑基准测试** — 默默已发样本清单，明天分头开跑。插件端(Hermes) + CLI端(Claude Code)，各测各的，交叉对比

### 🟡 P1 — 阻塞解除后可推进
- [ ] **cf1279d push 到远程** — GitHub 网络问题暂挂，网络恢复后推
- [x] **JitPack 直连** ✅ v0.2.1 已通，mavenLocal() 已去掉

### ⏸️ P2 — 待排期
- [ ] CallIndex 迁移 Phase 2（SQLite 表结构 + 查询方法迁 common）
- [ ] ProviderPreset 温度锁定 Phase 2（temperature=0.1）
- [ ] 模型特性标记 Phase 2
- [ ] Hermes v0.14.0 升级（需 stash 本地改动后跑 update）

### ⏳ 等待默默/喵呜
- [ ] Bug 3 需具体 case 再议（方法名精确匹配非 Bug，是设计选择）
