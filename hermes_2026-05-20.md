# Hermes 会话记忆 — 2026-05-20

## 工作内容

1. **三层交叉验证方案全量落地** — L1 PSIStructExtractor + StructContext注入Prompt + L2 OutputNormalizer + L3 EvidenceValidator+ConfidenceAnnotator，插件端三层全部完成并两端对齐
2. **EvidenceValidatorTest 修复** — Schema v2 后依赖名称匹配导致 testConfidence_High_When80PercentPass 失败，补充 sourceLines 字段名修复
3. **全量测试通过** — `./gradlew test` 96/96 PASS
4. **沙箱构建通过** — `./gradlew buildPlugin` BUILD SUCCESSFUL
5. **memory 瘦身** — 94% → 78%（移除 2 条旧记录，缩短 1 条）
6. **硬编码 ruoyi-system 删除** — `CodeLensAnalyzeAction.java:126`，改为通用提示
7. **豆包端点修复** — `ProviderPreset.java` 默认模型从失效 `ep-xxxxx` 改为 `doubao-seed-2-0-lite-260215`
8. **基准测试文档** — 创建 `C:\workspace\fmp\CodeLens-Benchmark-Spec.md`（12 文件分层抽样 + 评分模型 + 流程规范）

## 关键决策

| 决策 | 结论 | 拍板人 |
|------|------|--------|
| 三层交叉验证方案 | L1 PSI/JavaParser确定性提取→注入Prompt | L2 LLM分析+OutputNormalizer | L3 EvidenceValidator+ConfidenceAnnotator | 默默 |
| 基准测试流程 | 12文件分层抽样 + 评分模型 + 基准测试规范文档化 | 默默 |
| OutputNormalizer 工具类过滤 | Prompt 规则 19 + OutputNormalizer 双层过滤 → deps 稳定在 15-30 个 | 默默 |

## 待办（来自 ACTIVE.md）
- [ ] 需求 #5：calls 格式归一化 + type 枚举归一化（待喵呜出方案）
- [ ] 两端跑基准测试验证 Layer 1 效果
- [ ] common 独立模块（待喵呜确认分割方案）
- [ ] CallIndex 迁移 Phase 2（SQLite 表结构 + 查询方法迁 common）
- [ ] ProviderPreset 温度锁定 Phase 2（temperature=0.1）
- [ ] 模型特性标记 Phase 2

## 当前阶段
- **阶段**：Layer 1 底图提取 — 已完成，待后续排期
- **下一阶段**：两端跑基准测试 → Phase 2 排期
